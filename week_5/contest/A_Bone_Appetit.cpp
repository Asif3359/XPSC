#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N, M;
    cin >> N >> M;
    int X, Y;
    cin >> X >> Y;
    cout << (N * X) + (M * Y) << endl;

    return 0;
}
